# Backend Structure

- `src/bootstrap` — quét plugin/theme khi khởi động
- `src/core` — config, MySQL, logger
- `src/modules` — domain logic (auth, pages, blocks, themes, media, analytics, AI, ...)
- `src/shared` — guards, decorators, filters, interceptors
