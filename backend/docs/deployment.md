# Deployment

Development local:

- API: `localhost:3000`
- Swagger: `localhost:3000/api/docs`

Goi y production:

- Docker compose: api, worker, realtime, mysql, redis, r2-compatible storage, clickhouse, nginx
- CI/CD: lint, test, build, migrate, deploy
